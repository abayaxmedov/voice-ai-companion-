# Shared Contracts

This package mirrors backend DTOs for desktop/Unreal bridge code.

The Python contracts in `services/orchestrator/companion_core/contracts.py`
are the current source of truth. TypeScript contracts here should be generated
or manually kept aligned until schema generation is added.

